--- ebook-tools-0.2.2/cmake/FindLibZip.cmake.0001~	2012-09-04 16:32:32.000000000 +0200
+++ ebook-tools-0.2.2/cmake/FindLibZip.cmake	2013-08-14 17:01:23.206468980 +0200
@@ -15,17 +15,12 @@ if (LIBZIP_LIBRARY AND LIBZIP_INCLUDE_DI
   set(LIBZIP_FOUND TRUE)
 else (LIBZIP_LIBRARY AND LIBZIP_INCLUDE_DIR)
 
-  find_path(LIBZIP_INCLUDE_DIR zip.h
-    ${GNUWIN32_DIR}/include
-  )
-
-  find_library(LIBZIP_LIBRARY NAMES zip
-    PATHS
-    ${GNUWIN32_DIR}/lib
-  )
-
+  INCLUDE(FindPkgConfig)
+  PKG_CHECK_MODULES(LIBZIP libzip)
+  set(LIBZIP_INCLUDE_DIR ${LIBZIP_INCLUDE_DIRS})
+  set(LIBZIP_LIBRARY ${LIBZIP_LIBRARIES})
   include(FindPackageHandleStandardArgs)
-  FIND_PACKAGE_HANDLE_STANDARD_ARGS(LibZip DEFAULT_MSG LIBZIP_LIBRARY LIBZIP_INCLUDE_DIR)
+  FIND_PACKAGE_HANDLE_STANDARD_ARGS(LIBZIP DEFAULT_MSG LIBZIP_LIBRARY LIBZIP_INCLUDE_DIR)
 
     # ensure that they are cached
     set(LIBZIP_INCLUDE_DIR ${LIBZIP_INCLUDE_DIR} CACHE INTERNAL "The libzip include path")
